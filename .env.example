# The proxy reads S17Code's .env for the control token, so this file only needs
# to say where the engine is. The token itself is never copied here, and never
# reaches the browser.
S17_BASE_URL=http://127.0.0.1:8113
LUMEN_PORT=8115
